---
name: git-workflow
description: Compatibility pointer for older Claude/PA prompts. Canonical OpenCode workflow instructions live in .opencode/skills/git-workflow/SKILL.md.
---

# Compatibility Pointer

The canonical OpenCode workflow instructions live at:

- `.opencode/skills/git-workflow/SKILL.md`

This `.claude` file exists only so older ClaudeCode-compatible PA prompts can discover the canonical OpenCode instructions. Do not update branch strategy rules here.
